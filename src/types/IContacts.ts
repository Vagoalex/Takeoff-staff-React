export interface IContacts {
  id: number;
  firstName: string;
  secondName: string;
  email: string;
  number: number;
}

export interface IContact {
  firstName: string;
  secondName: string;
  email: string;
  number: number;
}
